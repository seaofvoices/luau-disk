name: Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - uses: seaofvoices/test-luau-package-action@v1
        with:
          package-manager: yarn
          skip-build: true
